---
label: Test
hide_body: true
fields:
- name: menu
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: pages
  label: Menu
pages:
- menus.md
